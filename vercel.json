{
  "installCommand": "npm install -g pnpm@10.26.1 && pnpm install",
  "buildCommand": "cd artifacts/geo-puzzle && pnpm run build && cd ../ && mkdir -p public && cp -R artifacts/geo-puzzle/dist/public/. public/",
  "outputDirectory": "public",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
